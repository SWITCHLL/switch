
/**
 * Notifications feature — Bounded Context
 *
 * Structure (to be implemented):
 *   components/  — NotificationBell, NotificationList, NotificationItem
 *   hooks/       — useNotifications, useUnreadCount
 *   actions/     — markAsRead, markAllAsRead, deleteNotification
 *   queries/     — getUserNotifications
 *   types.ts     — Notification, NotificationType
 */
export {}
