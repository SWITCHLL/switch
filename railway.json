{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.workers"
  },
  "deploy": {
    "startCommand": "npx tsx --conditions react-server workers/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
