/**
 * Services layer.
 *
 * Services encapsulate external API calls and third-party integrations.
 * They are framework-agnostic TypeScript modules consumed by:
 *   - Route handlers (app/api/)
 *   - Server Actions (features/actions.ts files)
 *   - Workers (workers/)
 */
